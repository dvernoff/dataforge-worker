{
  "id": "local-storage",
  "name": "Local Storage",
  "description": "Store and manage files on the local filesystem.",
  "version": "1.0.0",
  "runtime": "worker",
  "icon": "hard-drive",
  "settings": [
    {
      "key": "base_path",
      "label": "Base Path",
      "type": "string",
      "required": true,
      "default": "./storage",
      "placeholder": "/var/data/storage"
    }
  ]
}
